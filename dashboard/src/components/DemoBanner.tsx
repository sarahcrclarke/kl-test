export function DemoBanner() {
  return (
    <div className="demo-banner">
      <strong>Demo data.</strong> Everything below is synthetic placeholder content, not real competitor
      homepages. Run the <code>weekly-capture</code> GitHub Action (or <code>npm run capture</code> in
      <code>/scraper</code> from a machine with normal internet access) to replace it with real captures.
    </div>
  );
}
